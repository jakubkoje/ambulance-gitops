apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/jakubkoje/ambulance-webapi//deployments/kustomize/install
  - openapi-ui.service.yaml
  - http-route.yaml

patches:
  - path: patches/ambulance-webapi.deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/component: xjelinekj-ambulance-webapi
      wac-fiit.github.io/component: backend
